# 2026-03-04

## Spyglass IDX Lead Capture Issues

Trisha discovered that contact form submissions from spyglass-idx.vercel.app weren't reaching Follow Up Boss (FUB).

### Root Cause
- FUB API returning 401 authentication errors
- API keys start with `fka_` instead of standard FUB prefix `fub_`
- This suggests either:
  - Not actually using Follow Up Boss
  - Using a custom/white-label version
  - Keys are for a different CRM system

### Solution Status - NOT WORKING
The IDX lead backup system was partially implemented but is NOT functional:

1. **Mission Control** 
   - Migration file exists: `0011_idx_leads_backup.sql`
   - Code exists in repository for webhook endpoint
   - Dashboard page loads but has no data
   - Webhook endpoint returns 404 (not deployed/accessible)

2. **IDX Site**
   - NO Mission Control integration in production code
   - Only sends to Follow Up Boss (which fails)
   - Missing webhook functionality

### Current Status
- Lead backup system is NOT working
- All form submissions are being LOST
- FUB authentication failing (401 errors)
- No backup system capturing leads
- Only visible in Vercel function logs temporarily

### What Needs to be Fixed
1. Deploy/fix Mission Control webhook endpoint
2. Add Mission Control integration to IDX site
3. Resolve FUB authentication (`fka_` vs `fub_` keys)
4. Verify database migration has run

### Access (currently non-functional)
Dashboard: https://missioncontrol-tjfm.onrender.com/admin/dashboards/idx-leads

## Mission Control Tracking Scripts
Trisha working on adding tracking scripts to Mission Control.
Process documented in: ~/clawd/memory/mission-control-tracking-scripts-process.md