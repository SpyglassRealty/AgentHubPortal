# 2026-03-05 Memory Log

## Spyglass IDX Core Blocks Issue

### Problem
- Core blocks (core-hero, core-split, core-cards, core-testimonial, core-text) were added to Mission Control
- They appeared in the page builder but weren't rendering on the front-end
- Next.js was tree-shaking them out of the production bundle

### Pages Affected
- buyer-test - only showing idx-features and faq blocks
- test-core - only showing idx-reviews block

### Solution Attempts
1. Force deployment with comment change (74dd598) - didn't work
2. Add debug logging (c998204) - helped identify issue
3. Add explicit core block check (355fb5b) - didn't work
4. **Move core blocks to separate function (ea141ae)** - should work!

### Mission Control Issues Found
1. Page editor saves to wrong endpoint - tries PUT /api/pages/:slug instead of /api/admin/pages/:id
2. Need to use page ID for editing: https://missioncontrol-tjfm.onrender.com/admin/pages/[ID]/edit
3. test-core page ID: 38fab5ec-50e6-4e96-90fe-4e81af07a46e

### Working Blocks (confirmed in production)
- Basic: heading, text, image, button, spacer, divider, html
- Layout: columns
- Content: hero, cards, testimonial, cta, faq, image-gallery
- IDX: idx-hero, idx-stats, idx-cards, idx-testimonials, idx-reviews, idx-neighborhoods, idx-cta-banner, idx-two-column, idx-features

### Next Steps
- Wait for deployment of ea141ae to complete
- Test core blocks on test-core and buyer-test pages
- If still not working, may need to restructure imports/exports