# 2026-02-27 — Thursday

## Pulse Search Bar (Mission Control)
- Ryan requested search bar at top of Pulse page for zip, city, county filtering
- Built PulseSearchBar component with typeahead, keyboard nav, ARIA accessibility
- Backend: `/api/pulse/v2/search` endpoint with ZIP_META mapping for 65 Austin metro zips across 5 counties
- Maggie QA'd — fixed missing ARIA attributes, flagged fragile itemIndex pattern and mobile edge cases
- Deployed to Render via GitHub push to SpyglassRealty/AgentHubPortal main branch

## Pulse Filter UX Issues (Multiple Rounds)
- **Round 1:** Ryan couldn't tell if city/county filter was working — map didn't zoom, no visual feedback
  - Fix: Added auto-zoom via fitBounds, orange filter banner, subtitle update
- **Round 2:** Chart showed same data ($700K) for both Round Rock and Austin — identical metro-wide data regardless of filter
  - Root cause: HistoricalChart only received `selectedZip` (null when filtering by city), fell back to default zip 78704
  - Fix: Added `filteredZips` and `filterLabel` props, created `generateAveragedTimeseries()` that averages across filtered zips
  - Round Rock now shows ~$391K avg (3 zips: 78664, 78665, 78681), Austin shows higher
- **Round 3:** Ryan tested before Render deploy finished — still showed old data
  - Root cause: Render deploy takes 3-5 minutes, browser was caching old JS bundle
  - Lesson: **Always verify the deployed bundle contains the new code before telling Ryan it's done** — use `curl` to check bundle hash or search for function names in the JS
  - Verified by fetching the JS bundle and checking for `generateAveragedTimeseries` / `filteredZips`

## Key Lesson — VERIFY BEFORE REPORTING
Ryan explicitly called this out: "Can you please CHECK THIS before you tell me it's done?"
- Always hard-refresh and test the live deployed site before confirming
- Render deploys take 3-5+ minutes — don't assume the push = live
- Check the JS bundle hash changed, or search for new function/variable names in the bundle
- Use browser tool to actually interact with the feature

## Spyglass IDX — Mobile Search Fix
- AI Search bar on spyglass-idx.vercel.app had mobile issues: text clipping, oversized buttons, tight spacing
- Fix: Responsive padding (pr-[5.5rem] sm:pr-24), compact search button on mobile, shorter placeholder, tighter mode toggle
- Verified on 390px viewport via browser tool before confirming to Ryan
- Deployed via Vercel (faster than Render, ~1 min)

## AppFactory
- Ryan asked for login credentials: ryan@spyglassrealty.com / Spygla$$realty123! (from seed.ts)
- Live at https://appfactory-liart.vercel.app

## Nightly Autonomous Work (11 PM)

### Agent Retention Intelligence Dashboard
- Built standalone HTML dashboard: `projects/retention-intelligence/index.html`
- Flight risk scoring with 6 weighted factors (production drop, tenure milestone, engagement, market exposure, peer departure, split pressure)
- Sortable watchlist, agent detail modals with per-factor breakdown + recommended actions
- 4 retention playbooks (Coffee Check-In, Production Boost, Recognition, Split Review)
- Cohort retention heatmap, risk distribution chart, key insights panel
- CSV export for at-risk agents
- Currently simulated data — ready for Mission Control API integration

### Recruiting Outreach Generator
- Built standalone HTML tool: `projects/retention-intelligence/outreach-templates.html`
- 6 agent personas (Frustrated Producer, Growth Seeker, Top Producer, Team Leader, New Agent, Boomerang)
- 4 channels (Email, Text, Social DM, Voicemail) × 4 variants (Initial, FU1, FU2, Break-up)
- Customizable: agent name, current brokerage, volume level
- Copy-to-clipboard, best practices tips embedded

### Retention & Recruiting Playbook 2026
- Comprehensive strategy doc: `brain/concepts/retention-recruiting-playbook-2026.md`
- Retention signals, churn analysis, intervention playbooks
- Competitor weakness analysis: KW, Compass, eXp, REAL, Realty Austin
- 179→300 math: cutting churn from 20% to 10% saves 18 agents/year (1.5 months recruiting effort)
- Action items organized by this week / this month / this quarter

### Research
- "The Great Agent Reshuffle of 2026" — all 4 migration triggers present simultaneously
- HousingWire retention strategies validated our approach
- Austin market: $540K median (down 4.1%), buyer's market, rates 6.65%
- Competitors: REAL Brokerage most aggressive in Austin, targeting 1-2 year agents

## Cron Jobs Running
- GHL sync: every 4 hours (midnight, 4am, 8am, noon, 4pm, 8pm)
- Smart alerts: every 30 min
- GHL message polling: every 15 min  
- Morning brief delivered at 7am
- OpenClaw research delivered overnight (2am)

## CMS → IDX Blog Pipeline (Caleb)
- **TOC block fix**: Auto-populates from H2/H3 heading blocks in PageBuilder via useEffect
  - Also auto-generates anchorId from heading text in settings panel
  - Server-side: `populateTocBlocks()` fills TOC at import time
- **Strip .html from blog links**: `stripHtmlFromBlogLinks()` cleans href in text, html, button, image blocks during import
- **Blog CMS → IDX connection**: IDX blog now fetches from Mission Control API (`/api/landing-pages?pageType=blog`)
  - ISR revalidation every 5 minutes — no manual redeploy needed for new posts
  - `dynamicParams=true` — new CMS slugs work without rebuild
  - Cleared all old static blog posts from `blog-posts.json` (now empty array)
- **BlogSectionsRenderer**: New component on IDX side that properly renders page builder sections
  - Handles: hero, heading, text, image, button, toc, html, divider, faq blocks
  - CMS posts use sections renderer; legacy static posts use raw HTML
  - Fixed duplicate featured image (template's own + hero block)
  - Added bullet points for lists
- **View Live links**: Fixed to point to `spyglass-idx.vercel.app/blog/{slug}` for blog pages
- **Vercel auto-deploy not working**: Git pushes to spyglass-idx main don't auto-trigger Vercel builds — need `npx vercel --prod` manually
- **Note**: Render deploys take 3-5 min; Vercel deploys take ~4 min (4200+ pages)

## Blog Import — New Spreadsheet Columns (Late Afternoon Session with Caleb)
- Spreadsheet updated with 8 columns: Blog Title, URL Slug, Google Doc, Hero Image, **OG Image**, **Blog Photos**, **Date Published**, Date Crawled
- All link columns use HYPERLINK() formulas — must parse `cell.f` to extract URLs (not cell values or standard hyperlinks)
- **Updated import-sheet endpoint** to read columns E (OG Image), F (Blog Photos), G (Date Published)

### Blog Photos Folder Download
- Blog Photos column links to a Google Drive folder containing article images
- Old blog content referenced paths like `/uploads/agent-1/07 - Blogs/filename.jpg` — broken on new server
- **Solution**: Parse the Drive folder HTML page to extract filename→fileId mappings (scrape `\\x` escaped HTML, find fileIds preceding filenames)
- `listDriveFolderFiles()` and `downloadDriveImage()` helper functions added to blogRoutes.ts
- Images downloaded locally, old paths replaced in articleHtml before cheerio parses into page builder blocks
- fullContent also gets path replacement (must happen AFTER fullContent is defined, not before)

### OG Image Download
- OG Image (column E) = the featured/thumbnail image for blog listings
- Hero Image (column D) = the page banner background
- Initially ogImageUrl stored as Drive `uc?export=view` URL — Google blocks/redirects these as image sources
- **Fix**: Import now downloads OG image locally via `downloadDriveImage()`, stores local `/uploads/` path
- IDX blog-data.ts: `featuredImageUrl` priority = OG image first, hero image fallback
- `resolveImageUrl()` helper prefixes relative paths with CMS base URL

### Date Published
- Parsed from formats like "January 29th, 2026" — strip ordinal suffixes (st/nd/rd/th) before `new Date()`
- Spreadsheet date takes priority over Google Doc embedded date

### Key Mistake — Don't Rebuild What Exists
- I built a duplicate import-sheet endpoint + dialog UI from scratch
- Caleb pointed out `/admin/blog/import` page already existed (blog-import.tsx) with full UI
- The existing endpoint at line 825 was already sophisticated (cheerio parsing, page builder blocks, hero download, TOC, etc.)
- **Lesson**: Always check existing code/routes before building new features. Ask "is this already built?" 
- Had to remove my duplicate endpoint and update the existing one instead

### Deployment Notes
- Render: Mission Control repo → auto-deploys on push to main (3-5 min)
- Vercel: `npx vercel --prod --yes` from spyglass-idx dir (auto-deploy still broken)
- ISR (5 min revalidation) means new published blogs appear on Vercel without redeploy
- But code changes (like image priority logic) require a full Vercel deploy

## Projects/Repos Reference
- Mission Control: ~/clawd/projects/mission-control → github.com/SpyglassRealty/AgentHubPortal → Render (missioncontrol-tjfm.onrender.com)
- Spyglass IDX: ~/clawd/spyglass-idx → github.com/SpyglassRealty/spyglass-idx → Vercel (spyglass-idx.vercel.app)
- AppFactory: ~/clawd/projects/appfactory → Vercel (appfactory-liart.vercel.app)

## Blog Image Fix (Evening Session with Caleb)
- Caleb published Austin Economic Forecast 2026 blog via Mission Control CMS
- Blog images kept breaking due to two issues:
  1. **Render cold starts** — ISR revalidation hitting sleeping Render server, caching empty results
  2. **Render ephemeral storage** — uploaded images wiped on Render restart/sleep
- **Fixes deployed to spyglass-idx:**
  - `fetchWithRetry()` — retry logic for CMS API calls (handles Render cold starts)
  - `/api/revalidate` — on-demand ISR revalidation endpoint (GET /api/revalidate?path=/blog)
  - `/api/img` — image proxy that fetches from Render, retries on cold starts, serves through Vercel CDN with 24h cache + 7-day stale-while-revalidate
  - All blog image URLs (featured, hero, inline content) rewritten to go through `/api/img` proxy
  - Updated both `blog-data.ts` and `BlogSectionsRenderer.tsx`
- Vercel CLI deploy was needed (auto-deploy from GitHub wasn't triggering for unknown reason)
- Caleb confirmed images working through Vercel CDN
- Build optimization (reducing 4200+ static community pages) discussed but deferred
- **Key commits:** c53e377, 85e7cb4, a25d4d5, 022621d (all on main branch)
