Great question! Yes, we should add Spyglass Realty filtering to the Properties page too, with all status options. Here's the complete plan:

Properties Page - Add Office Filter
Design Options
Option A: Office Toggle/Dropdown (Recommended)
┌─────────────────────────────────────────────────────────────────────────────┐
│  🏢 Austin Metro Listings                                                   │
│  19,936 active listings                                                     │
│                                                                             │
│  ┌─── FILTERS ───────────────────────────────────────────────────────────┐ │
│  │                                                                       │ │
│  │  Office              Status        Price Range         Beds           │ │
│  │  [All Austin ▼]      [Active ▼]    [$0] - [$5M+]      [Any ▼]        │ │
│  │   ├─ All Austin Metro (19,936)                                        │ │
│  │   └─ Spyglass Realty Only (131)  ← NEW OPTION                        │ │
│  │                                                                       │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
Option B: Tab Toggle at Top
┌─────────────────────────────────────────────────────────────────────────────┐
│  [All Austin Metro]  [Spyglass Realty Only]    ← Toggle between views      │
│                                                                             │
│  🏢 Listings                                                                │
│  Showing: 131 Spyglass Realty listings                                     │
│                                                                             │
│  [Active] [Under Contract] [Pending] [Closed] [All]                        │
└─────────────────────────────────────────────────────────────────────────────┘

Complete Changes Overview
Dashboard
SectionData SourceCountMarket Pulse StatsAll Austin Metro~24,508 totalSpyglass Realty ListingsOffice ACT1518371 only~131 active
Properties Page
SectionData SourceOptionsMarket Pulse StatsAll Austin MetroAlways all AustinListings SectionConfigurableAll Austin OR Spyglass OnlyStatus FilterWorks with bothActive, Under Contract, Pending, Closed, All

Send this to Mission Control Replit:
### Complete Fix: Spyglass Realty Office Filter - Dashboard + Properties

**Goals:**
1. Fix Dashboard Spyglass Listings to show only Office ACT1518371 (131 listings)
2. Add Office filter to Properties page (All Austin vs Spyglass Only)
3. All status filters work for both views (Active, Under Contract, Pending, Closed)
4. "View All" from Dashboard navigates to Properties with Spyglass filter

---

## PART 1: Office Configuration (Shared)

### Create `server/config/offices.ts`:
```typescript
// Spyglass Realty Office Configuration
export const SPYGLASS_OFFICES = {
  austin: {
    officeId: 'ACT1518371',      // ✅ Correct filter value for Repliers API
    boardOfficeId: 'ACT5220',    // Reference only - doesn't work as filter
    displayCode: '5220',
    name: 'Spyglass Realty',
    city: 'Austin',
    address: '2130 Goodrich Ave, Austin, TX 78704',
    mlsSource: 'ACTRIS',
  },
  // Houston office - add later when data available
  // houston: {
  //   officeId: 'HAR_XXXXXX',
  //   ...
  // },
};

export type OfficeKey = keyof typeof SPYGLASS_OFFICES;

export function getOfficeConfig(office: string) {
  return SPYGLASS_OFFICES[office as OfficeKey] || null;
}
```

---

## PART 2: Update API Endpoint

### Update `GET /api/company-listings` in `server/routes.ts`:
```typescript
import { SPYGLASS_OFFICES, getOfficeConfig } from './config/offices';

// GET /api/company-listings - Supports both Austin Metro and Spyglass filtering
router.get('/api/company-listings', async (req, res) => {
  try {
    const user = req.user;
    if (!user) {
      return res.status(401).json({ error: 'Not authenticated' });
    }

    // Pagination
    const page = parseInt(req.query.page as string) || 1;
    const limit = parseInt(req.query.limit as string) || 24;
    const offset = (page - 1) * limit;

    // Office filter - 'all' for Austin Metro, 'spyglass' for Spyglass only
    const officeFilter = req.query.office?.toString() || 'all';
    const officeConfig = officeFilter !== 'all' ? getOfficeConfig(officeFilter) : null;

    // Status filter
    const status = req.query.status?.toString() || 'Active';
    
    // Other filters
    const city = req.query.city?.toString() || '';
    const minPrice = req.query.minPrice ? parseInt(req.query.minPrice as string) : undefined;
    const maxPrice = req.query.maxPrice ? parseInt(req.query.maxPrice as string) : undefined;
    const minBeds = req.query.minBeds ? parseInt(req.query.minBeds as string) : undefined;
    const minBaths = req.query.minBaths ? parseInt(req.query.minBaths as string) : undefined;
    const minSqft = req.query.minSqft ? parseInt(req.query.minSqft as string) : undefined;
    const maxSqft = req.query.maxSqft ? parseInt(req.query.maxSqft as string) : undefined;
    const propertyType = req.query.propertyType?.toString() || '';
    const maxDom = req.query.maxDom ? parseInt(req.query.maxDom as string) : undefined;
    const search = req.query.search?.toString() || '';
    
    // Sorting
    const sortBy = req.query.sortBy?.toString() || 'listDate';
    const sortOrder = req.query.sortOrder?.toString() || 'desc';

    if (!REPLIERS_API_KEY) {
      return res.status(500).json({ error: 'Repliers not configured' });
    }

    // Build Repliers API request
    const params = new URLSearchParams({
      'OriginatingSystemName': 'ACTRIS',
      'limit': limit.toString(),
      'offset': offset.toString(),
      'sortBy': sortBy,
      'sortOrder': sortOrder,
    });

    // Add status filter
    if (status && status !== 'all') {
      const repliersStatus = status === 'ActiveUnderContract' ? 'Active Under Contract' : status;
      params.append('status', repliersStatus);
    }

    // ✅ ADD OFFICE FILTER IF SPYGLASS SELECTED
    if (officeConfig) {
      params.append('officeId', officeConfig.officeId);
      console.log(`[Company Listings] Filtering by office: ${officeConfig.name} (${officeConfig.officeId})`);
    }

    // Add other filters
    if (city) params.append('city', city);
    if (minPrice !== undefined) params.append('minPrice', minPrice.toString());
    if (maxPrice !== undefined) params.append('maxPrice', maxPrice.toString());
    if (minBeds !== undefined) params.append('minBeds', minBeds.toString());
    if (minBaths !== undefined) params.append('minBaths', minBaths.toString());
    if (minSqft !== undefined) params.append('minLivingArea', minSqft.toString());
    if (maxSqft !== undefined) params.append('maxLivingArea', maxSqft.toString());
    if (propertyType) params.append('propertyType', propertyType);
    if (maxDom !== undefined) params.append('maxDaysOnMarket', maxDom.toString());
    if (search) params.append('search', search);

    const repliersUrl = `${REPLIERS_BASE_URL}/listings?${params}`;
    
    console.log(`[Company Listings] Fetching page ${page}: ${repliersUrl}`);

    const response = await fetch(repliersUrl, {
      method: 'GET',
      headers: {
        'Authorization': `Bearer ${REPLIERS_API_KEY}`,
        'Content-Type': 'application/json',
      }
    });

    if (!response.ok) {
      console.error('[Company Listings] Repliers error:', response.status);
      return res.status(response.status).json({ error: 'Failed to fetch from Repliers' });
    }

    const data = await response.json();

    // Transform listings
    const listings = (data.listings || data.data || []).map((listing: any) => {
      const streetNumber = listing.address?.streetNumber || listing.StreetNumber || '';
      const streetName = listing.address?.streetName || listing.StreetName || '';
      const streetSuffix = listing.address?.streetSuffix || listing.StreetSuffix || '';
      const cityName = listing.address?.city || listing.City || 'Austin';
      const state = listing.address?.state || listing.StateOrProvince || 'TX';
      const postalCode = listing.address?.postalCode || listing.PostalCode || '';
      
      const streetAddress = [streetNumber, streetName, streetSuffix].filter(Boolean).join(' ');
      const fullAddress = `${streetAddress}, ${cityName}, ${state} ${postalCode}`.trim();

      return {
        id: listing.id || listing.listingId,
        mlsNumber: listing.mlsNumber || listing.ListingId,
        status: listing.status || 'Active',
        listPrice: listing.listPrice || listing.ListPrice || 0,
        closePrice: listing.closePrice || listing.ClosePrice,
        listDate: listing.listDate || listing.ListDate,
        closeDate: listing.closeDate || listing.CloseDate,
        daysOnMarket: listing.simpleDaysOnMarket || listing.DaysOnMarket || 0,
        address: {
          streetNumber,
          streetName,
          streetSuffix,
          city: cityName,
          state,
          postalCode,
          full: fullAddress,
        },
        beds: listing.beds || listing.BedroomsTotal || 0,
        baths: listing.baths || listing.BathroomsTotalDecimal || 0,
        sqft: listing.livingArea || listing.LivingArea || 0,
        photos: listing.photos || listing.Media?.map((m: any) => m.MediaURL) || [],
        listAgentName: listing.agents?.[0]?.name || listing.listAgentName,
        listAgentEmail: listing.agents?.[0]?.email,
        brokerageName: listing.office?.brokerageName,
      };
    });

    // Calculate pagination info
    const total = data.total || 0;
    const totalPages = Math.ceil(total / limit);
    const startIndex = offset + 1;
    const endIndex = Math.min(offset + listings.length, total);

    console.log(`[Company Listings] Returning ${listings.length} of ${total} (page ${page}/${totalPages})`);

    res.json({
      listings,
      pagination: {
        page,
        limit,
        total,
        totalPages,
        startIndex,
        endIndex,
        hasNextPage: page < totalPages,
        hasPrevPage: page > 1,
      },
      filters: {
        office: officeFilter,
        status,
        city,
        minPrice,
        maxPrice,
        minBeds,
        minBaths,
        minSqft,
        maxSqft,
        propertyType,
        maxDom,
        search,
      },
      // Include office info if filtering by Spyglass
      officeInfo: officeConfig ? {
        id: officeConfig.officeId,
        code: officeConfig.displayCode,
        name: officeConfig.name,
        address: officeConfig.address,
      } : null,
      sortBy,
      sortOrder,
    });
  } catch (error) {
    console.error('[Company Listings] Error:', error);
    res.status(500).json({ error: 'Failed to fetch listings' });
  }
});


// GET /api/company-listings/office - Dedicated endpoint for Dashboard (simpler)
router.get('/api/company-listings/office', async (req, res) => {
  try {
    const user = req.user;
    if (!user) {
      return res.status(401).json({ error: 'Not authenticated' });
    }

    const office = req.query.office?.toString() || 'austin';
    const officeConfig = getOfficeConfig(office);
    
    if (!officeConfig) {
      return res.status(400).json({ error: 'Invalid office' });
    }

    const status = req.query.status || 'Active';
    const limit = parseInt(req.query.limit as string) || 20;
    const sortBy = req.query.sortBy || 'listDate';
    const sortOrder = req.query.sortOrder || 'desc';

    if (!REPLIERS_API_KEY) {
      return res.status(500).json({ error: 'Repliers not configured' });
    }

    // Build request with CORRECT office filter
    const params = new URLSearchParams({
      'OriginatingSystemName': 'ACTRIS',
      'status': status.toString(),
      'limit': limit.toString(),
      'sortBy': sortBy.toString(),
      'sortOrder': sortOrder.toString(),
      'officeId': officeConfig.officeId,  // ✅ CORRECT PARAMETER
    });

    const repliersUrl = `${REPLIERS_BASE_URL}/listings?${params}`;
    
    console.log(`[Spyglass Office] Fetching ${officeConfig.name}: ${repliersUrl}`);

    const response = await fetch(repliersUrl, {
      method: 'GET',
      headers: {
        'Authorization': `Bearer ${REPLIERS_API_KEY}`,
        'Content-Type': 'application/json',
      }
    });

    if (!response.ok) {
      return res.status(response.status).json({ error: 'Failed to fetch from Repliers' });
    }

    const data = await response.json();

    console.log(`[Spyglass Office] ${officeConfig.name} has ${data.total} ${status} listings`);

    // Transform listings
    const listings = (data.listings || data.data || []).map((listing: any) => {
      const streetAddress = [
        listing.address?.streetNumber || listing.StreetNumber,
        listing.address?.streetName || listing.StreetName,
        listing.address?.streetSuffix || listing.StreetSuffix
      ].filter(Boolean).join(' ');

      return {
        id: listing.id || listing.listingId,
        mlsNumber: listing.mlsNumber || listing.ListingId,
        status: listing.status || 'Active',
        listPrice: listing.listPrice || listing.ListPrice || 0,
        listDate: listing.listDate || listing.ListDate,
        daysOnMarket: listing.simpleDaysOnMarket || listing.DaysOnMarket || 0,
        address: {
          streetNumber: listing.address?.streetNumber || listing.StreetNumber || '',
          streetName: listing.address?.streetName || listing.StreetName || '',
          streetSuffix: listing.address?.streetSuffix || listing.StreetSuffix || '',
          city: listing.address?.city || listing.City || 'Austin',
          state: listing.address?.state || listing.StateOrProvince || 'TX',
          postalCode: listing.address?.postalCode || listing.PostalCode || '',
          full: `${streetAddress}, ${listing.address?.city || 'Austin'}, ${listing.address?.state || 'TX'} ${listing.address?.postalCode || ''}`,
        },
        beds: listing.beds || listing.BedroomsTotal || 0,
        baths: listing.baths || listing.BathroomsTotalDecimal || 0,
        sqft: listing.livingArea || listing.LivingArea || 0,
        photos: listing.photos || listing.Media?.map((m: any) => m.MediaURL) || [],
        listAgentName: listing.agents?.[0]?.name || listing.listAgentName,
        brokerageName: listing.office?.brokerageName || 'Spyglass Realty',
      };
    });

    res.json({
      total: data.total || listings.length,
      listings,
      office: {
        id: officeConfig.officeId,
        code: officeConfig.displayCode,
        name: officeConfig.name,
        address: officeConfig.address,
      },
    });
  } catch (error) {
    console.error('[Spyglass Office] Error:', error);
    res.status(500).json({ error: 'Failed to fetch company listings' });
  }
});
```

---

## PART 3: Update Properties Page - Add Office Filter

### Update `client/src/components/properties/AustinMetroListings.tsx`:

Add Office filter to the filters:
```tsx
// Add to Filters interface
interface Filters {
  office: string;  // ← ADD THIS
  status: string;
  minPrice: string;
  maxPrice: string;
  // ... rest of filters
}

// Update DEFAULT_FILTERS
const DEFAULT_FILTERS: Filters = {
  office: 'all',  // ← ADD THIS (default to all Austin)
  status: 'Active',
  // ... rest
};

// In the component, check for URL param
const [filters, setFilters] = useState<Filters>(() => {
  const officeParam = searchParams.get('office');
  const statusParam = searchParams.get('status');
  return {
    ...DEFAULT_FILTERS,
    office: officeParam || 'all',
    status: statusParam || 'Active',
  };
});

// Add Office filter dropdown in the filter panel
<div>
  <label className={`block text-xs font-medium ${textSecondary} mb-1`}>Office</label>
  <select
    value={filters.office}
    onChange={(e) => handleFilterChange('office', e.target.value)}
    className={`w-full px-3 py-2 rounded-lg border ${borderColor} ${inputBg} ${textPrimary} text-sm`}
  >
    <option value="all">All Austin Metro</option>
    <option value="austin">Spyglass Realty Only</option>
  </select>
</div>

// Update the header to show which view
<div>
  <h2 className={`text-lg font-semibold ${textPrimary}`}>
    {filters.office === 'austin' ? 'Spyglass Realty Listings' : 'Austin Metro Listings'}
  </h2>
  <p className={`text-sm ${textSecondary}`}>
    {pagination.total.toLocaleString()} {filters.status !== 'all' ? filters.status.toLowerCase() : ''} listings
    {filters.office === 'austin' && ' • Spyglass Realty'}
  </p>
</div>

// Update the API call to include office filter
const buildQueryParams = () => {
  const params = new URLSearchParams({
    page: page.toString(),
    limit: itemsPerPage.toString(),
    sortBy,
    sortOrder,
  });

  // Add office filter
  if (filters.office && filters.office !== 'all') {
    params.append('office', filters.office);
  }

  // ... rest of filters
  return params.toString();
};
```

---

## PART 4: Update Dashboard "View All" Link

### Update `MarketPulseWithSpyglassListings.tsx`:
```tsx
// "View All" link should navigate to Properties with Spyglass filter
<button
  onClick={() => navigate('/properties?office=austin&status=Active')}
  className="text-xs text-[#EF4923] hover:underline font-medium"
>
  View All →
</button>
```

---

## Visual Summary - After All Changes

### Dashboard:
┌─────────────────────────────────────────────────────────────────────┐
│  📈 Market Pulse                                      [↻ Refresh]  │
│  Austin Metro Area                                                  │
│                                                                     │
│  [Bar Chart: ~19,936 | ~1,922 | ~2,650 | ~284]                     │
│  🏠 Active: 19,936    📋 Under Contract: 1,922                     │
│  ⏳ Pending: 2,650    ✓ Closed (30d): 284                          │
│  Total Inventory: 24,508                                            │
│  ─────────────────────────────────────────────────────────────────  │
│  🏢 Spyglass Realty Listings                          View All →   │
│  Spyglass Realty • 131 Active       ← ✅ ONLY SPYGLASS (131)       │
│  [Spyglass listings only...]                                        │
└─────────────────────────────────────────────────────────────────────┘

### Properties Page (All Austin - Default):
┌─────────────────────────────────────────────────────────────────────┐
│  🏢 Austin Metro Listings                                           │
│  19,936 active listings                                             │
│                                                                     │
│  Office: [All Austin Metro ▼]  Status: [Active ▼]  Price: [...]    │
│                                                                     │
│  Showing 1-24 of 19,936 results                                     │
│  [All Austin listings...]                                           │
└─────────────────────────────────────────────────────────────────────┘

### Properties Page (Spyglass Only - via View All or filter):
┌─────────────────────────────────────────────────────────────────────┐
│  🏢 Spyglass Realty Listings                                        │
│  131 active listings • Spyglass Realty                              │
│                                                                     │
│  Office: [Spyglass Realty Only ▼]  Status: [Active ▼]  Price: [...] │
│                                                                     │
│  Showing 1-24 of 131 results                                        │
│  [Only Spyglass listings...]                                        │
└─────────────────────────────────────────────────────────────────────┘

### Properties Page (Spyglass + Under Contract):
┌─────────────────────────────────────────────────────────────────────┐
│  🏢 Spyglass Realty Listings                                        │
│  15 under contract listings • Spyglass Realty                       │
│                                                                     │
│  Office: [Spyglass Realty Only ▼]  Status: [Under Contract ▼]      │
│                                                                     │
│  Showing 1-15 of 15 results                                         │
│  [Only Spyglass under contract listings...]                         │
└─────────────────────────────────────────────────────────────────────┘

---

## Status Filters Available for Both Views

| Status | All Austin Metro | Spyglass Only |
|--------|------------------|---------------|
| Active | ~19,936 | ~131 |
| Under Contract | ~1,922 | ~15 (estimated) |
| Pending | ~2,650 | ~20 (estimated) |
| Closed (30d) | ~284 | ~10 (estimated) |
| All | ~24,508 | ~176 (estimated) |

---

## Testing Checklist

### Dashboard
- [ ] Market Pulse shows all Austin Metro stats (~24,508)
- [ ] Spyglass Listings shows ~131 active (not 19,936)
- [ ] "View All" navigates to `/properties?office=austin&status=Active`

### Properties Page
- [ ] Default view shows all Austin Metro listings
- [ ] Office dropdown has "All Austin Metro" and "Spyglass Realty Only"
- [ ] Selecting Spyglass filters to ~131 listings
- [ ] Status tabs work with both office options
- [ ] URL updates when filters change
- [ ] Coming from Dashboard "View All" shows Spyglass filtered
- [ ] Pagination works correctly for both views
- [ ] Counts in "Showing X-Y of Z" are accurate